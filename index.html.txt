<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  />

  <title>A Special Invitation 💌</title>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
      font-family: "Trebuchet MS", Arial, sans-serif;
      color: #64364a;
      background:
        radial-gradient(circle at top left, #fff4f8, transparent 35%),
        radial-gradient(circle at bottom right, #ffd8e7, transparent 40%),
        linear-gradient(135deg, #ffe6ef, #fff7dc);
    }

    .card {
      position: relative;
      z-index: 2;
      width: min(100%, 480px);
      padding: 42px 30px;
      text-align: center;
      border: 2px solid rgba(255, 255, 255, 0.8);
      border-radius: 28px;
      background: rgba(255, 255, 255, 0.78);
      box-shadow: 0 20px 60px rgba(157, 72, 105, 0.18);
      backdrop-filter: blur(14px);
      animation: cardEntrance 0.9s ease;
    }

    .envelope {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 82px;
      height: 82px;
      margin-bottom: 18px;
      border-radius: 50%;
      font-size: 44px;
      background: #ffe1eb;
      box-shadow: 0 10px 25px rgba(214, 93, 140, 0.18);
      animation: bounce 2s infinite;
    }

    .eyebrow {
      margin-bottom: 10px;
      font-size: 0.82rem;
      font-weight: 700;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: #d85d8f;
    }

    h1 {
      margin-bottom: 14px;
      font-size: clamp(2rem, 7vw, 3.1rem);
      line-height: 1.05;
      color: #9f3f68;
    }

    .message {
      max-width: 390px;
      margin: 0 auto 24px;
      font-size: 1rem;
      line-height: 1.7;
      color: #6d5260;
    }

    .date-details {
      margin-bottom: 26px;
      padding: 18px;
      border: 1px dashed #eda9c1;
      border-radius: 18px;
      background: rgba(255, 240, 246, 0.72);
    }

    .detail {
      margin: 7px 0;
      font-size: 0.98rem;
    }

    .detail strong {
      color: #a4436b;
    }

    .button-area {
      position: relative;
      min-height: 62px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 14px;
    }

    button {
      border: none;
      border-radius: 999px;
      padding: 14px 25px;
      font-size: 1rem;
      font-weight: 700;
      cursor: pointer;
      transition:
        transform 0.2s ease,
        box-shadow 0.2s ease,
        background 0.2s ease;
    }

    #yesButton {
      color: white;
      background: linear-gradient(135deg, #e9689b, #bd4c78);
      box-shadow: 0 9px 22px rgba(199, 70, 121, 0.3);
    }

    #yesButton:hover {
      transform: translateY(-3px) scale(1.04);
      box-shadow: 0 13px 28px rgba(199, 70, 121, 0.38);
    }

    #noButton {
      position: relative;
      color: #a04469;
      background: #ffe2ec;
    }

    .response {
      display: none;
      margin-top: 24px;
      padding: 18px;
      border-radius: 18px;
      font-size: 1.05rem;
      line-height: 1.5;
      color: #8c365c;
      background: #fff0f6;
      animation: popIn 0.5s ease;
    }

    .heart {
      position: fixed;
      bottom: -40px;
      z-index: 1;
      pointer-events: none;
      opacity: 0.75;
      animation: floatUp linear forwards;
    }

    .signature {
      margin-top: 22px;
      font-size: 0.9rem;
      font-style: italic;
      color: #a86b83;
    }

    @keyframes cardEntrance {
      from {
        opacity: 0;
        transform: translateY(30px) scale(0.96);
      }

      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    @keyframes bounce {
      0%,
      100% {
        transform: translateY(0) rotate(-3deg);
      }

      50% {
        transform: translateY(-8px) rotate(3deg);
      }
    }

    @keyframes floatUp {
      from {
        transform: translateY(0) rotate(0deg);
        opacity: 0;
      }

      15% {
        opacity: 0.8;
      }

      to {
        transform: translateY(-115vh) rotate(360deg);
        opacity: 0;
      }
    }

    @keyframes popIn {
      from {
        opacity: 0;
        transform: scale(0.85);
      }

      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    @media (max-width: 500px) {
      .card {
        padding: 34px 20px;
      }

      .button-area {
        flex-direction: column;
        min-height: 120px;
      }

      button {
        width: 180px;
      }
    }
  </style>
</head>

<body>
  <main class="card">
    <div class="envelope">💌</div>

    <p class="eyebrow">A little question for you</p>

    <!-- Change their name here -->
    <h1>Will you go on a date with me?</h1>

    <!-- Change the invitation message here -->
    <p class="message">
      I would love to spend a cute evening with you, eat something delicious,
      laugh too much, and make another memory together.
    </p>

    <!-- Change the date details here -->
    <section class="date-details">
      <p class="detail">
        <strong>📅 When:</strong> Saturday, August 1 at 6:30 PM
      </p>

      <p class="detail">
        <strong>📍 Where:</strong> It’s a surprise!
      </p>

      <p class="detail">
        <strong>👗 Dress code:</strong> Cute and comfortable
      </p>
    </section>

    <div class="button-area" id="buttonArea">
      <button id="yesButton">Yes, of course! 💗</button>
      <button id="noButton">No 🙈</button>
    </div>

    <div class="response" id="response">
      Yay! It’s officially a date 🥰<br />
      I can’t wait to see you!
    </div>

    <!-- Change your name here -->
    <p class="signature">Made with love, Brian ♡</p>
  </main>

  <script>
    const yesButton = document.getElementById("yesButton");
    const noButton = document.getElementById("noButton");
    const buttonArea = document.getElementById("buttonArea");
    const response = document.getElementById("response");

    const noButtonMessages = [
      "Are you sure? 🥺",
      "Think again 💭",
      "Pretty please? 🌷",
      "Wrong button 😌",
      "One more chance! 💕"
    ];

    let noAttempts = 0;

    yesButton.addEventListener("click", () => {
      buttonArea.style.display = "none";
      response.style.display = "block";

      createCelebrationHearts();
    });

    function moveNoButton() {
      const card = document.querySelector(".card");
      const cardRect = card.getBoundingClientRect();
      const buttonRect = noButton.getBoundingClientRect();

      const maximumX = cardRect.width - buttonRect.width - 40;
      const maximumY = 120;

      const randomX = Math.max(
        0,
        Math.floor(Math.random() * maximumX)
      );

      const randomY =
        Math.floor(Math.random() * maximumY) - maximumY / 2;

      noButton.style.position = "absolute";
      noButton.style.left = `${randomX}px`;
      noButton.style.top = `${randomY + 55}px`;

      noButton.textContent =
        noButtonMessages[noAttempts % noButtonMessages.length];

      noAttempts += 1;
    }

    noButton.addEventListener("mouseenter", moveNoButton);
    noButton.addEventListener("click", moveNoButton);
    noButton.addEventListener("touchstart", moveNoButton);

    function createHeart() {
      const heart = document.createElement("div");

      const heartOptions = ["💗", "💕", "💖", "🌸", "✨"];

      heart.className = "heart";
      heart.textContent =
        heartOptions[Math.floor(Math.random() * heartOptions.length)];

      heart.style.left = `${Math.random() * 100}vw`;
      heart.style.fontSize = `${16 + Math.random() * 20}px`;
      heart.style.animationDuration = `${5 + Math.random() * 5}s`;

      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 10000);
    }

    function createCelebrationHearts() {
      for (let i = 0; i < 35; i += 1) {
        setTimeout(createHeart, i * 70);
      }
    }

    setInterval(createHeart, 850);
  </script>
</body>
</html>